import { OrdenIngresoPipe } from './orden-ingreso.pipe';

describe('OrdenIngresoPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenIngresoPipe();
    expect(pipe).toBeTruthy();
  });
});
